````bash
pip install -U tf-nightly tb-nightly tensorflow_datasets
pip install -U coloredlogs colorterm
pip install shapely imgaug cython pydot graphviz scipy
pip install termcolor tabulate tqdm msgpack msgpack-numpy pyzmq psutil  # Tensorpack
pip install ipykernel
python -m ipykernel install --user --name=tf20
````
